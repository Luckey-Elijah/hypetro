import 'package:meta/meta.dart';

/// {@template view_model}
/// Contains the data that views use to generate UI content.
/// {@endtemplate}
@immutable
abstract class ViewModel {
  /// {@macro view_model}
  const ViewModel();
}
